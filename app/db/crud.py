"""
app/db/crud.py — Async CRUD operations for conversation history
================================================================
Functions:
  - save_message(user_id, role, text)
  - get_history(user_id, limit)
  - clear_history(user_id)

TODO (Крок 9): implement all three functions
"""
