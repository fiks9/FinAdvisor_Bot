# FinAdvisor Bot — Python Dependencies
# Versions pinned to avoid "works on my machine" surprises.
# Run: pip install -r requirements.txt
#
# NOTE: chromadb 0.6.x does NOT have a Windows pre-built wheel for chroma-hnswlib.
# We pin 1.5.1+ which ships as a bundled win_amd64 wheel (no C++ Build Tools needed).

# --- Telegram ---
python-telegram-bot==21.6

# --- LLM: Groq + LangChain ---
langchain==0.3.14
langchain-groq==0.2.3
langchain-community==0.3.14
groq==0.13.1

# --- Embeddings (local, no API cost) ---
sentence-transformers==3.3.1
langchain-huggingface==0.1.2

# --- Vector DB (1.5.1+ has pre-built Windows wheel — no C++ compiler needed) ---
chromadb==1.5.1
langchain-chroma==0.2.4

# --- Database ---
aiosqlite==0.20.0

# --- Config & Validation ---
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
